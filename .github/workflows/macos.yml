name: macOS

on:
  push:
  pull_request:

jobs:
  ci:
    uses: ./.github/workflows/native.yml
    with:
      runs-on: macos-14
      nx: |
        pnpm exec nx run-many -t test --output-style=stream
        pnpm exec nx run examples:build
        pnpm exec nx run examples:build-software
