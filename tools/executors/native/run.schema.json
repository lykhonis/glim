{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "preset": { "type": "string", "description": "CMake preset whose build dir contains the binary" },
    "binary": {
      "type": "string",
      "description": "Path to the executable or .app bundle, relative to build/<preset>/"
    },
    "args": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Arguments forwarded to the example"
    },
    "simulator": {
      "type": "string",
      "enum": ["ios", "tvos"],
      "description": "If set, install and launch the .app on an iOS or tvOS simulator via simctl"
    },
    "bundleId": {
      "type": "string",
      "description": "CFBundleIdentifier of the app (required when simulator is set)"
    },
    "device": {
      "type": "string",
      "description": "Optional iOS simulator name, Android AVD name, or adb serial"
    },
    "android": {
      "type": "boolean",
      "description": "If true, boot an emulator if needed, then adb install and launch the APK"
    },
    "packageName": {
      "type": "string",
      "description": "Android applicationId (required when android is true)"
    }
  },
  "required": ["binary"]
}
