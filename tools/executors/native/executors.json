{
  "executors": {
    "cmake": {
      "implementation": "./cmake.js",
      "schema": "./cmake.schema.json",
      "description": "Configure and build a CMake target. Optionally installs into dist/. Does not cache the CMake build tree."
    },
    "run": {
      "implementation": "./run.js",
      "schema": "./run.schema.json",
      "description": "Run a built native example from build/<preset>/, or install and launch an .app on the iOS/tvOS simulator."
    }
  }
}
