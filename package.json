{
  "name": "glim",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "nx build examples",
    "test": "nx test core",
    "hello": "nx run hello:run",
    "examples": "nx run examples:run"
  },
  "devDependencies": {
    "@glim/native": "workspace:*",
    "nx": "^20.3.0"
  }
}
